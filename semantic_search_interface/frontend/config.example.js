// Copy this file to `config.js` and paste your Cloudflare Worker's PROXY_SECRET.
// `config.js` is gitignored, so the secret never lands in the repo — and it's
// not exposed in the interface's properties panel either.
export const PROXY_SECRET = '';
